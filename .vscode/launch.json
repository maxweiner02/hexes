{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Odin (Windows / MSVC)",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${workspaceFolder}\\build\\debug.exe",
      "cwd": "${workspaceFolder}",
      "args": [],
      "console": "integratedTerminal",
      "preLaunchTask": "Build"
    }
  ]
}